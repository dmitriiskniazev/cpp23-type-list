## Summary

<!-- What changed and why? -->

## Checklist

- [ ] `./scripts/ci.sh` passes locally (or `cmake --build build && ctest --test-dir build`)
- [ ] `./scripts/format.sh` run if C++ sources changed
- [ ] New partition registered in `CMakeLists.txt`, `modules/type_list.cppm`, and `docs/API.md` (if applicable)
- [ ] Tests and examples added for new operations (if applicable)

## Test plan

<!-- How was this verified? -->
