## Summary

<!-- What changed and why? -->

## Checklist

- [ ] `./scripts/ci.sh` passes locally (or `cmake --build build && ctest --test-dir build`)
- [ ] `./scripts/format.sh` run if C++ under `libs/` changed
- [ ] type-list: partition in `libs/type-list/CMakeLists.txt`, `modules/type_list.cppm`, `docs/API.md` (if applicable)
- [ ] Tests and examples added for new operations (if applicable)

## Test plan

<!-- How was this verified? -->
